# How to publish this profile README

1. Go to https://github.com/new and create a **public** repository named exactly `0xadriank` (same as your username).
   - GitHub will show: "✨ Special repository — its README will appear on your public profile."
2. Add the `README.md` from this zip to the root of that repository and commit to `main`.
3. Visit https://github.com/0xadriank — the README now renders at the top of your profile.

## Recommended profile settings (Settings → Public profile)

- **Name:** Adrian Koster
- **Bio:** `AI Specialist & Developer — ML, NLP/LLMs, Computer Vision. I ship production-ready AI systems.`
- **Location:** Poland
- **Website:** https://www.freelancer.com/u/adriank06
- Upload a professional avatar.

## Recommended repo polish for `ragserve`

- **Description:** `Self-contained RAG service — stdlib + SQLite core, optional OpenAI backend, CLI + FastAPI`
- **Topics:** `rag`, `llm`, `retrieval-augmented-generation`, `fastapi`, `sqlite`, `python`
- Pin `ragserve` on your profile (Customize your pins).
- Tag a release, e.g. `v0.1.0`.

## Note on the stats card

The README includes a GitHub stats card (github-readme-stats). It updates automatically.
If you prefer a cleaner look while your account is new (few commits/stars), just delete the `## 📈 Stats` section from `README.md`.
